---
"@ai-hero/sandcastle": minor
---

Add a `codex-afk` init profile with isolated ChatGPT subscription
authentication, branch-local CodeGraph caches, and generated Docker/runtime
integration. Promote tracer-bullet TDD into the implementation scaffold,
filter planner candidates through explicit eligibility and parallel-safety
rules, strengthen specification-aware review with read-only coding standards,
and verify merged branches through a cumulative batch gate. For Node/npm
projects, generate a machine-readable project profile, a separate feedback-loop
contract, sandbox-native dependency bootstrap with an npm cache, and executable
package and cumulative verification scripts. For Android/Gradle projects,
detect the Android plugin and compile SDK, install JDK 17 plus the required
Android SDK packages, mount an isolated Gradle cache, and generate emulator-free
unit-test, lint, and debug-assembly gates.
